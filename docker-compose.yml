services:
  localstack:
    image: localstack/localstack:3
    ports:
      - "4566:4566"
    environment:
      - DEBUG=1
      - LAMBDA_DOCKER_NETWORK=
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
